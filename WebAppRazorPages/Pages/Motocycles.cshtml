@page
@model Project.Pages.MotocyclesModel
@{
    ViewData["Title"] = "Motocycles page";
}

<html>
    <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" >
    <style>
        body{
            padding: 20px;
        }

        .car-card {
            border: 1px solid #dee2e6;
            border-radius: 4px;
            padding: 20px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Страница информации о ТС</h1>
        <a asp-page="Edit" class="btn btn-primary btn-create mb-3">Создать</a>
        <div class="row">
            @foreach (var Motocycle in Model.motocycles)
            {
                <div class="col-md-4">
                    <div class="car-card">
                        <p><strong>ID:</strong>@Motocycle.Id</p>
                        <p><strong>BrandMotocycle:</strong>@Motocycle.BrandMotocycle</p>
                        <p><strong>Model:</strong>@Motocycle.Model</p>
                        <p><strong>EngineMotocycle:</strong>@Motocycle.EngineMotocycle</p>
                        <div>
                            <a asp-page="EditMotocycle" asp-route-id="@Motocycle.Id" class="btn btn-secondary">Изменить</a>
                            <form asp-page-handler="Delete" method="post" asp-route-id="@Motocycle.Id" class="delete-form">
                                <button type="submit" class="btn btn-danger delete-button">Удалить</button>
                            </form>
                        </div>   
                    </div>
                </div>
            }
        </div>
    </div>
</body>
</html>